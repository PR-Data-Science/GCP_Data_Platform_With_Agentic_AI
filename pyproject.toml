[project]
name = "llm-feedback-pipeline"
version = "0.1.0"
description = "GCP pipeline for LLM feedback ingestion, transformation, and loading"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Your Team" }]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
